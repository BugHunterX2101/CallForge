import { AppShell } from "@/components/app-shell"; import { integrations } from "@/lib/demo-data";
export default function Settings(){return <AppShell><main className="page"><div className="settings"><h1 className="heading">Settings & integrations</h1><p className="desc">Connect your real workspace. Demo records are never mixed with connected data.</p><article className="card"><h2>Workspace setup</h2>{[['CRM system','HubSpot or Google Sheets fallback'],['Transcript tool','Zoom'],['Recap destination','Slack channel or direct message'],['Transcript folder','Google Drive folder']].map(([title,copy])=><div className="settingrow" key={title}><div><b>{title}</b><p>{copy}</p></div><button>Configure</button></div>)}</article><article className="card" style={{marginTop:28}}><h2>Connected services</h2>{integrations.map(x=><div className="settingrow" key={x.provider}><div className="source-icon"><span className="material-symbols-outlined">{x.provider==='Slack'?'forum':x.provider==='HubSpot'?'hub':'account_circle'}</span></div><div><b>{x.provider}</b><p>{x.connected?'Connected in demo workspace':'Not connected'}</p></div><button>{x.connected?'Reconnect':'Connect'}</button></div>)}</article></div></main></AppShell>}
