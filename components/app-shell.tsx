"use client";
import Link from "next/link"; import { usePathname } from "next/navigation";
const nav=[['/pipeline','Deals'],['/activity','Contacts'],['/activity','Call Notes'],['/tasks','Tasks']];
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();return <div className="app"><nav className="nav"><Link href="/activity" className="brand"><span className="logo">G</span>Gravity <span className="demo">DEMO WORKSPACE</span></Link><div className="links">{nav.map(([href,label])=><Link key={label} href={href} className={(label==='Call Notes'&&path==='/activity')||(label==='Deals'&&path==='/pipeline')||(label==='Tasks'&&path==='/tasks')?'active':''}>{label}</Link>)}</div><div className="actions"><Link href="/pipeline" className="darkbtn">Create Deal</Link><button className="iconbtn" aria-label="Notifications"><span className="material-symbols-outlined">notifications</span></button><button className="iconbtn" aria-label="Help"><span className="material-symbols-outlined">help</span></button><div className="avatar">DR</div></div></nav>{children}</div>}
