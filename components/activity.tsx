"use client";
import Link from "next/link"; import { calls, integrations } from "@/lib/demo-data";
export function Activity(){const call=calls[0];return <main className="page"><section className="hero"><div><h1>Activity Feed</h1><p>You&apos;ve saved roughly <b className="mint">2h 15m</b> today through automated call logging and drafting.</p></div><div className="stats"><div><span>CALLS LOGGED</span><strong>12</strong></div><div><span>DRAFTS SENT</span><strong>4</strong></div></div></section><section className="grid"><article className="card"><div className="approval-head"><div className="source-icon"><span className="material-symbols-outlined">videocam</span></div><div><div className="title">Acme Corp Intro</div><div className="sub">Zoom • 10:30 AM</div></div><span className="chip draft">Draft Ready</span></div><p className="message">&quot;Hi Sarah, great connecting today. As discussed, I&apos;ve attached the pricing proposal for the Q3 rollout. Let me know if you need any adjustments before Friday.&quot;</p><div className="buttonrow"><Link href={`/calls/${call.id}`}><button>Edit</button></Link><Link href={`/calls/${call.id}`}><button className="primary">▷ Approve</button></Link></div></article><article className="card"><div className="approval-head"><div className="source-icon" style={{color:'#10b981',background:'#effcf6'}}><span className="material-symbols-outlined">phone</span></div><div><div className="title">Stark Ind. Sync</div><div className="sub">Phone • Yesterday</div></div><span className="chip input">Input Needed</span></div><p className="message" style={{background:'#fff5f5',borderColor:'#fecaca'}}><b style={{color:'#dc2626'}}>Missing Info:</b> Confirmed implementation date not found in transcript.</p><div className="buttonrow"><button className="primary">Provide Input</button></div></article><article className="card"><h2>Integrations</h2>{integrations.slice(0,3).map(x=><div className="integration" key={x.provider}><div className="source-icon"><span className="material-symbols-outlined">{x.provider==='Zoom'?'videocam':x.provider==='Gmail'?'mail':'calendar_month'}</span></div><span className="title" style={{fontSize:18}}>{x.provider}</span><span className="chip live">● Connected</span></div>)}<Link href="/settings" className="integration" style={{border:'1px dashed #dbe4ee',borderRadius:12,marginTop:18,textDecoration:'none'}}><div className="source-icon">＋</div><span className="sub">Add Integration</span></Link></article><article className="recent"><div className="topline"><h2>Recent activity</h2><span className="sub">View All</span></div>{[['Initech Follow-up','Gmail','10:45 AM','Logged'],['Q2 Strategy Meeting.mp4','Drive','09:12 AM','Processing'],['Globex Initial Pitch','Zoom','Yesterday','Logged']].map(([a,b,c,d])=><div className="row" key={a}><div className="source-icon"><span className="material-symbols-outlined">{b==='Drive'?'folder':'mail'}</span></div><div><div className="title">{a}</div><div className="sub">{b} · {c}</div></div><span className="spacer"/><span className={`chip ${d==='Logged'?'live':'draft'}`}>● {d}</span><span className="dots">⋮</span></div>)}</article></section></main>}
